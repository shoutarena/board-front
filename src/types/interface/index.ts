import BoardList from 'types/interface/board-list.interface'
import FavoriteList from 'types/interface/favorite-list.interface'
import CommentList from 'types/interface/comment-list.interface'

export type {
    BoardList,
    FavoriteList,
    CommentList,
}
